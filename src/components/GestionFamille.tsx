import { Copy, Lock, Plus, Save, Trash2, Users } from 'lucide-react';
import { useEffect, useState } from 'react';
import { creerInvitation, lireFamille, modifierEnfants, retirerMembre, useMembresFamille } from '../firebase/useFamilles';
import { familleIdCourante } from '../firebase/familleCourante';
import type { Famille } from '../types';
import { planFamille } from '../types';
import { UpgradeNotice } from './UpgradeNotice';

export function GestionFamille({ userId }: { userId: string }) {
  const [famille, setFamille] = useState<Famille | null>(null);
  const [enfants, setEnfants] = useState<string[]>([]);
  const [code, setCode] = useState<string | null>(null);
  const [message, setMessage] = useState<string | null>(null);
  const familleId = familleIdCourante();
  const membres = useMembresFamille(familleId);
  const estProprietaire = membres.some((membre) => membre.userId === userId && membre.role === 'proprietaire');
  const premium = planFamille(famille) === 'payant';
  useEffect(() => { if (familleId) void lireFamille(familleId).then((value) => { setFamille(value); setEnfants(value?.enfants ?? []); }); }, [familleId]);
  async function save() { if (!familleId) return; if (!premium && enfants.filter((item) => item.trim()).length > 1) { setMessage('La formule gratuite est limitée à un enfant.'); return; } await modifierEnfants(familleId, enfants); setMessage('Enfants enregistrés.'); }
  async function invite() { if (!familleId) return; const value = await creerInvitation(familleId, userId); setCode(value); setMessage('Code créé, partage-le avec l’autre parent.'); }
  if (!famille) return null;
  return <section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="text-lg font-black">{famille.nom}</h3><p className="mt-1 flex items-center gap-2 text-sm font-bold text-slate-500"><Users size={16} /> Gestion de la famille</p><div className="mt-4"><span className="font-black">Enfants suivis</span><div className="mt-2 space-y-2">{enfants.map((enfant, index) => <div key={index} className={`flex gap-2 ${!premium && index > 0 ? 'opacity-60' : ''}`}><input value={enfant} disabled={!premium && index > 0} onChange={(event) => setEnfants((items) => items.map((item, itemIndex) => itemIndex === index ? event.target.value : item))} className="min-w-0 flex-1 rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 font-bold disabled:cursor-not-allowed" />{enfants.length > 1 && <button type="button" onClick={() => setEnfants((items) => items.filter((_, itemIndex) => itemIndex !== index))} className="rounded-2xl bg-rose-50 px-3 text-rose-600" aria-label="Supprimer cet enfant"><Trash2 size={18} /></button>}{!premium && index > 0 && <span className="inline-flex items-center gap-1 rounded-2xl bg-slate-100 px-3 text-xs font-black"><Lock size={14} /> Archivé</span>}</div>)}{(premium || enfants.length === 0) && <button type="button" onClick={() => setEnfants((items) => [...items, ''])} className="inline-flex items-center gap-2 rounded-2xl bg-slate-100 px-3 py-2 text-sm font-black"><Plus size={16} /> Ajouter un enfant</button>}</div></div>{!premium && enfants.length >= 1 && <div className="mt-3"><UpgradeNotice>La formule gratuite permet de suivre un enfant.</UpgradeNotice></div>}<div className="mt-4 flex flex-wrap gap-2"><button type="button" onClick={() => void save()} className="inline-flex items-center gap-2 rounded-2xl bg-slate-950 px-4 py-3 font-black text-white"><Save size={17} /> Enregistrer</button><button type="button" onClick={() => void invite()} disabled={!premium && membres.filter((membre) => membre.role === 'invite' && !membre.bloqueParPlan).length >= 1} className="inline-flex items-center gap-2 rounded-2xl bg-cyan-100 px-4 py-3 font-black text-cyan-800 disabled:bg-slate-100 disabled:text-slate-400"><Users size={17} /> Inviter un parent</button></div>{!premium && membres.filter((membre) => membre.role === 'invite' && !membre.bloqueParPlan).length >= 1 && <div className="mt-3"><UpgradeNotice>La formule gratuite est limitée à un membre invité.</UpgradeNotice></div>}{code && <div className="mt-4 flex items-center justify-between rounded-2xl bg-emerald-50 p-4"><span><span className="block text-xs font-black uppercase text-emerald-700">Code d’invitation</span><strong className="text-2xl tracking-widest text-emerald-900">{code}</strong></span><button type="button" onClick={() => void navigator.clipboard?.writeText(code)} className="rounded-xl bg-white p-2 text-emerald-700" aria-label="Copier le code"><Copy size={18} /></button></div>}<div className="mt-5 border-t border-slate-100 pt-4"><p className="font-black">Membres ({membres.length})</p>{membres.map((membre) => <div key={membre.userId} className={`mt-2 flex items-center justify-between rounded-xl bg-slate-50 px-3 py-2 text-sm ${membre.bloqueParPlan ? 'opacity-60' : ''}`}><span className="font-bold">{membre.email} · {membre.role}{membre.bloqueParPlan ? ' · archivé' : ''}</span>{estProprietaire && membre.role === 'invite' && <button type="button" onClick={() => { if (window.confirm('Retirer ce membre ?')) void retirerMembre(famille.id, membre.userId); }} className="font-black text-rose-600">Retirer</button>}</div>)}</div>{message && <p className="mt-3 text-sm font-bold text-slate-600">{message}</p>}</section>;
}
